# Railway Environment Variables Template for CollabSpace
# Copy this file and fill in your values when setting up Railway

# ===== BACKEND SERVICE =====
# These variables should be set in your Backend service on Railway

# Server Configuration
NODE_ENV=production
PORT=9000

# JWT Secret (generate a strong random string)
JWT_SECRET=your_jwt_secret_here

# PostgreSQL (Railway will auto-populate these if you use Railway's Postgres)
# Use the Railway-provided DATABASE_URL or individual variables:
DATABASE_URL=${{Postgres.DATABASE_URL}}
# Or use individual variables:
# DB_HOST=${{Postgres.PGHOST}}
# DB_PORT=${{Postgres.PGPORT}}
# DB_NAME=${{Postgres.PGDATABASE}}
# DB_USER=${{Postgres.PGUSER}}
# DB_PASSWORD=${{Postgres.PGPASSWORD}}

# MongoDB (Railway will auto-populate if you use Railway's MongoDB)
MONGO_URI=${{MongoDB.MONGO_URL}}

# Redis (Railway will auto-populate if you use Railway's Redis)
REDIS_URL=${{Redis.REDIS_URL}}

# ===== FRONTEND SERVICE =====
# These variables should be set in your Frontend service on Railway

# Backend API URL (use Railway's internal networking)
NEXT_PUBLIC_API_URL=https://your-backend-service.up.railway.app
# Or for internal networking: ${{Backend.RAILWAY_PRIVATE_DOMAIN}}:9000

# WebSocket URL for real-time features
NEXT_PUBLIC_WS_URL=wss://your-backend-service.up.railway.app
